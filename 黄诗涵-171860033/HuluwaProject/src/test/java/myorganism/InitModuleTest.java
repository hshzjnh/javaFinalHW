package myorganism;

//import static org.junit.jupiter.api.Assertions.*;

import java.util.Vector;

import org.junit.jupiter.api.AfterAll;
import org.junit.jupiter.api.AfterEach;
import org.junit.jupiter.api.BeforeAll;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;


class InitModuleTest {
	
	@BeforeAll
	static void setUpBeforeClass() throws Exception {
	//	initModule.initCharacters();
	}

	@AfterAll
	static void tearDownAfterClass() throws Exception {
	}

	@BeforeEach
	void setUp() throws Exception {
	}

	@AfterEach
	void tearDown() throws Exception {
	}
	public static void notNULL(Organism obj) {
     //   if (obj == null) {
       //    fail("null");
       // }
    }
	@Test
	void testInit() {
	
		//this.notNULL(initModule.getRed());
	/*	notNull(initModule.getOrange());
		notNull(initModule.getYellow());
		notNull(initModule.getGreen());
		notNull(initModule.getIndigo());
		notNull(initModule.getBlue());
		notNull(initModule.getPurple());
		notNull(initModule.getGrandfather());
		notNull(initModule.getScorpion());
		notNull(initModule.getSnack());
		Vector<Monster> monsters=initModule.getMonsters();
		for(int i=0;i<monsters.size();i++)
			notNull(monsters.get(i));*/
		
	}

}
